version: "3.8"
services:
  webhook-server:
    networks:
      - meta-agency-stack_meta_net
      - openclaw-lan

networks:
  meta-agency-stack_meta_net:
    external: true
  openclaw-lan:
    external: true
